// ioBroker prettier template configuration file
// Reuses the shared prettier config that ships with @iobroker/eslint-config
import prettierConfig from "@iobroker/eslint-config/prettier.config.mjs";

export default prettierConfig;
